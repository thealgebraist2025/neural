name: Spell build and Run

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build with gcc
      run: gcc -O0 -Wall -g -std=c99 spell.c -lm -o spell

    - name: Run the spell program
      run: echo spell | lldb /bin/true

    - name: Upload Artifact
      uses: actions/upload-artifact@v4 # Use the latest version
      with:
        name: spellnetwork-diagram
        path: network.svg
