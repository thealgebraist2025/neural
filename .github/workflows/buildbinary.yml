name: Build binary 

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Build
      run: clang -fsanitize=address,undefined,leak -fno-omit-frame-pointer -O0 -Wall -g -std=c99 binary.c -lm -o binary 

    - name: Run the program
      run: ./binary 

    - name: Upload Artifact
      uses: actions/upload-artifact@v4 # Use the latest version
      with:
        name: binary-graph
        path: binary.svg